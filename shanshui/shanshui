//generative shanshui homepage
//create a new one on mouse mouseClicked
//
//
var mnts = 2;           //how many mountains (13, 13, 13)
var rvrs = 1;           //how many rivers (51, 102, 204)
var seals = 4;          //how many seals (179, 0, 0)


function preload() {

}


function setup() {
  createCanvas(windowWidth, windowHeight);

  textSize(30);
  textAlign(CENTER, CENTER);
}



function draw() {
  background(225, 225, 208);
  //header
  text("syd low",CENTER, CENTER);
  //buttons..

  //generate mountains, draw

  //generate rivers, draw

  //generate seals, draw

}

//buttons need to work, iff not on button regenerate
function mouseClicked(){

}
